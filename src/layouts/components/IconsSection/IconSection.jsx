import { Box } from '@mui/material';

function IconSection({ ...props }) {
  return (
    <Box>
      <span className="icon-[svg-spinners--wifi] text-blue-200" />
    </Box>
  );
}

export default IconSection;
